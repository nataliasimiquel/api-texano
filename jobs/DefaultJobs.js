import schedule from 'node-schedule'
import moment from 'moment'

export const DefaultJobs = ({io} = {}) => {
    // schedule.scheduleJob('*/5 * * * * *', async () => {
    //     console.log("each 5 seconds")

    // })
}